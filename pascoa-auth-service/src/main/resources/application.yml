server:
  port: 8081

spring:
  application:
    name: pascoa-auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/pascoa_auth
    username: postgres
    password: sua_senha
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  data:
    redis:
      host: localhost
      port: 6379
      password: pascoa123

  rabbitmq:
    host: localhost
    port: 5672
    username: pascoa
    password: pascoa123
    listener:
      simple:
        retry:
          enabled: true
          max-attempts: 3

auth:
  jwt:
    secret: "ALTERE-ESTA-CHAVE-SECRETA-MINIMO-32-CARACTERES-PROD"
    access-ttl: 15m
    refresh-ttl: 7d
  # S-02 — cookies httpOnly para JWT. Defaults seguros para produção (HTTPS, Strict).
  # Em dev local sobre HTTP, exportar AUTH_COOKIE_SECURE=false.
  cookie:
    secure: ${AUTH_COOKIE_SECURE:true}
    same-site: ${AUTH_COOKIE_SAMESITE:Strict}
    domain: ${AUTH_COOKIE_DOMAIN:}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    enabled: ${EUREKA_ENABLED:false}
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always

logging:
  level:
    br.com.seuprojeto.pascoa.auth: DEBUG
