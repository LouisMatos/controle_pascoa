# Configurações específicas do auth-service
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/pascoa_auth
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      password: ${REDIS_PASSWORD:pascoa123}

auth:
  jwt:
    secret: ${JWT_SECRET:ALTERE-ESTA-CHAVE-SECRETA-MINIMO-32-CARACTERES-PROD}
    access-ttl: 15m
    refresh-ttl: 7d

server:
  port: 8081
